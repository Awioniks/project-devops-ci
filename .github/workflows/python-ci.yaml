name: CI/CD for http-checker

on:
  push:
    branches: ['main']
  workflow_dispatch:

jobs:
  build-and-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Display Python version and location
        run: |
          which python